import React from "react";

/**
 * Placeholder for the TSX app shared in chat.
 *
 * The full component is intentionally not auto-inserted into this Ruby example
 * project because there is no React build setup in this repository yet.
 */
export default function HupoApp() {
  return (
    <main>
      <h1>Hupo App Source Received</h1>
      <p>
        Add a React/Vite/Next.js setup to run the full TSX component that was
        provided.
      </p>
    </main>
  );
}
